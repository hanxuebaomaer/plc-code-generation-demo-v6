"""Neutral, editable demonstration requirements; not vehicle commissioning rules."""

SCENARIOS = [
    {
        "id": "cab", "title": "操作端判断", "tag": "占用 · 冲突处理", "icon": "cab",
        "description": "双端钥匙占用判定，保留先激活端，冲突时输出封锁请求。",
        "requirement": """编写操作端判断控制模块，用于双司机室的离线逻辑演示。
输入：A端钥匙 xKeyA、B端钥匙 xKeyB、复位 xReset，均为 BOOL。
输出：A端有效 xActiveA、B端有效 xActiveB、冲突 xConflict、牵引封锁请求 xTractionBlocked。
逻辑要求：
1. 上电无占用。只有一端钥匙有效时，选中该端；同时首次激活两端时不选中任何端并报冲突。
2. 已有一端占用时，另一端激活不能抢占。保留先激活端，冲突置位并封锁牵引。
3. 后激活端退出后解除冲突；原占用端退出时先取消占用，若另一端仍有效，不得自动接管，必须等两端均退出后重新选择。
4. 复位只在两端钥匙均退出时有效；输出不得出现两端同时有效。
5. 无有效操作端或存在冲突时，牵引封锁请求为 TRUE。明确内部状态和初始值。
仅输出完整代码及必要中文注释，不映射真实硬件。""",
    },
    {
        "id": "direction", "title": "机车方向控制", "tag": "方向 · 零位联锁", "icon": "direction",
        "description": "运行中保持方向，停车回零后再允许切换。",
        "requirement": """编写机车方向选择控制模块，用于离线逻辑演示。
输入：操作端有效 xCabActive、操作端冲突 xCabConflict、速度 rSpeedKmh、前进请求 xForward、后退请求 xReverse。
输出：前进选定 xForwardSelected、后退选定 xReverseSelected、方向冲突 xDirectionConflict、牵引允许请求 xTractionPermit。
1. 初始方向为零位。速度绝对值不大于 1 km/h 视为静止，本阈值作为可配置输入，默认 1.0。
2. 仅在操作端有效且无冲突、机车静止并已经观察到方向手柄零位（两请求均 FALSE）后，允许选择新方向。
3. 运行中改变方向请求不改变当前选定方向；停车后必须先回零，再接受新方向。
4. 前后请求同时 TRUE 报冲突且禁止牵引；选定方向输出始终互斥。操作端失效或冲突时取消选定方向并重新等待零位。
5. 牵引允许请求仅在有效方向、有效操作端及无冲突时成立。负阈值或非法速度须给出明确保护处理。
使用清晰状态机，说明扫描周期调用方式，不操作真实设备。""",
    },
    {
        "id": "sanding", "title": "撒砂控制", "tag": "请求仲裁 · 方向分配", "icon": "sand",
        "description": "人工、自动和紧急制动请求分别判定并按方向输出。",
        "requirement": """编写机车撒砂控制模块，用于离线逻辑演示。
输入：人工脚踏 xManual、自动请求 xAutoRequest、自动取消 xAutoDisabled、紧急制动 xEmergency、速度 rSpeedKmh、前进选定 xForward、后退选定 xReverse、系统允许 xEnabled。
输出：前向撒砂 xSandForward、后向撒砂 xSandReverse、方向异常 xDirectionFault。
1. 自动取消仅屏蔽自动请求，不屏蔽人工脚踏和紧急制动请求。
2. 紧急制动撒砂仅在速度绝对值 15 至 80 km/h（包含端点）时有效。
3. 有效请求 = 人工脚踏 OR 未被取消的自动请求 OR 满足速度条件的紧急制动请求。
4. 系统允许且恰好选中一个方向时，按该方向输出；无方向或双方向冲突时两路撒砂均关闭。
5. 双方向同时选中时置方向异常。失能或请求消失时本周期关闭输出，不锁存。
写出完整接口、变量及所有分支；不增加热继电器等无关条件。""",
    },
    {
        "id": "parking", "title": "机车停放制动", "tag": "施加 · 缓解 · 反馈", "icon": "brake",
        "description": "施加优先、低速缓解及隔离确认，区分请求与反馈。",
        "requirement": """编写机车停放制动请求管理模块，用于离线逻辑演示，不直接驱动制动设备。
输入：施加按钮 xApply、缓解按钮 xRelease、操作端钥匙 xKeyOn、静止 xStandstill、隔离阀 xIsolated、隔离确认按钮 xConfirm、停放已施加反馈 xAppliedFeedback、停放已缓解反馈 xReleasedFeedback。
输出：施加请求 xApplyRequest、缓解请求 xReleaseRequest、牵引封锁请求 xTractionBlocked、隔离提示 xIsolationNotice、反馈异常 xFeedbackFault。
1. 默认保持施加请求。钥匙断开或施加按钮有效时施加优先；两按钮同时按下不得缓解。
2. 只有钥匙有效、机车静止、未隔离且无反馈冲突时才受理缓解按钮。两个请求互斥并保持到下一次有效指令。
3. 两反馈同时 TRUE 为异常，封锁牵引并取消缓解请求；请求不等于动作成功。
4. 静止且隔离时，必须在本次隔离发生后观察到确认按钮上升沿才允许解除隔离导致的牵引封锁。退出隔离后清除确认记忆。
5. 运行中出现隔离只给提示，不仅因隔离而新增牵引封锁；已施加反馈、施加请求或反馈异常仍封锁牵引。
6. 未隔离时未取得可靠缓解反馈不得允许牵引。明确优先级和隔离分支的局限。
不要实现真实气路或假定硬件动作成功。""",
    },
    {
        "id": "mileage", "title": "里程计算", "tag": "周期积分 · 数据保护", "icon": "distance",
        "description": "按速度和实际采样周期累积，区分总里程与区间里程。",
        "requirement": """编写里程计算模块，用于离线数据处理演示。
输入：使能 xEnabled、速度 rSpeedKmh（可正可负）、采样周期 rDeltaSeconds、区间清零 xResetTrip。
输出：总里程 rTotalKm、区间里程 rTripKm、输入异常 xInputInvalid。
1. 使用双精度累计值，单位 km。每个有效周期增量 = ABS(rSpeedKmh) * rDeltaSeconds / 3600。
2. 仅在使能、0 < rDeltaSeconds <= 1.0、ABS(rSpeedKmh) <= 200.0 且输入有限时累积；异常输入不增加里程并置异常。输入异常标志独立于使能：失能时仍报告非法输入，但有效输入不能仅因失能被报为异常。
3. 区间清零只影响区间里程，不改变总里程；清零优先，该周期区间保持零，总里程仍按有效采样累积。
4. 失能时不累积。明确掉电保持由外部存储层实现，本模块不虚构持久化能力。
5. ST 使用 LREAL，C++ 使用 double；避免单位错误和整数除法。仅输出完整代码与必要说明注释。""",
    },
    {
        "id": "lubrication", "title": "轮缘润滑及测试", "tag": "距离触发 · 测试时序", "icon": "lubrication",
        "description": "运行距离触发喷脂，静止时开展限时测试并支持退出。",
        "requirement": """编写轮缘润滑及测试模块，用于离线时序演示。
输入：使能、速度 km/h、采样周期秒、方向前进/后退、测试启动、测试取消；参数：间隔距离默认 300 m、喷脂时间默认 6 s。
输出：前向喷脂、后向喷脂、测试中、参数异常。
1. 自动模式下仅在速度绝对值 > 3 km/h 且方向唯一时累积距离，达到设定间隔后喷脂一次；本次喷脂期间不重复触发。
2. 自动模式的距离间隔范围 50 至 900 m；喷脂时间范围 1.5 至 10 s。无效参数关闭输出并报告异常，不静默使用错误参数。
3. 测试仅允许在静止（速度绝对值 <= 0.1 km/h）时由启动上升沿进入，先等待 20 s 后喷脂 6 s，再等待 20 s 后重复；测试总时长 300 s 后退出。
4. 测试期间前后两路同时喷脂仅作为离线试验信号；自动模式两路必须按方向互斥。
5. 取消、失能、测试期间速度超过静止阈值均立即退出测试并关闭输出；当周期优先处理退出条件，不能又触发喷脂。
6. 合法周期为 0 < dt <= 1 s；非法周期或非有限速度须置参数异常、退出测试并关闭输出。用明确状态机和周期累计计时，不使用阻塞等待。启动边沿每周期更新，测试启动持续按住不能重复启动。
声明全部接口及内部变量，说明边界和优先级，不映射真实执行器。""",
    },
]


def public_scenarios():
    return SCENARIOS
