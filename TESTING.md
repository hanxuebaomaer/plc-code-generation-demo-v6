# V8 交付前功能检查记录

日期：2026-09-21。检查对象为本目录源代码，不改变 V7 或旧公网服务。

## 自动回归

在 Python 3.12 独立环境运行 `python -m unittest discover -s tests -v`：27 项通过。

覆盖内容：健康接口与静态资源、同源策略、六个示例、语言路由隔离、缓存隔离、增量检查先于完整结果、结构不完整/断流识别、精确匹配的离线参考、普通问候、统一六项报告、需求证据与汇总结论一致性、TXT/DOCX/文字型PDF文件提取、空文件/超限/非法文件拒绝、无文字PDF提示、请求体限长、兼容接口。

JavaScript 使用 Node 语法检查通过。自动测试使用明确的测试替身，不把测试替身的“通过”当成实际生成代码正确性的证据。

## 在线模型功能抽查

使用已有的服务器模型配置，实际完成了六类需求的生成与审查请求：操作端判断（ST）、机车方向控制（C++）、撒砂控制（ST）、机车停放制动（C++）、里程计算（ST）、轮缘润滑及测试（C++）。

- 六类均取得完整代码；该轮输出长度约63–380行，随需求复杂度变化，并非硬性长度指标。
- 返回内容来自模型生成；重复的轮缘润滑请求命中完整需求缓存后再次审查。
- 非缓存请求的首段输出实测约0.6–0.8秒，完整生成与审查约8–16秒。仅记录当次环境表现，不作为速度承诺。
- 检查结论包含待复核项，不人为修改为全部通过；辅助审查本身可能存在误报、漏报。

## 页面检查

已在浏览器查看页面排版和控制项；验证语言切换、流式代码显示、步骤状态、检测项与结果对应、TXT 导入后填入文本框及旧结果失效。复制代码和C++导出按钮均实测返回成功提示。没有向浏览器提供模型密钥。

## 检查边界

没有声称取得目标 PLC 编译、C++ 编译器全量编译、动态仿真、实车测试或安全认证。模型代码仍需工程人员在目标工具链中复核。

## 公网部署核验

2026-09-21，独立服务已在 Render 部署为 Live，公网地址为 https://industrial-code-demo-v8.onrender.com/ 。健康接口返回 version `8.0.0`，ST/C++ 均可用，模型配置就绪；首页 HTTP 状态为 200。旧站继续使用原分支，未改动旧站配置。

在新网址上实际完成普通问候、TXT 文件导入以及六类场景的生成与完整辅助审查。六次请求均返回来源为模型的完整代码及六项检查报告；流式返回包含生成期间的增量检查事件，不是预置结果。

| 场景 | 语言 | 本次代码行数 | 首段输出（秒） | 生成及审查总用时（秒） |
| --- | --- | ---: | ---: | ---: |
| 操作端判断 | ST | 162 | 1.39 | 16.05 |
| 机车方向控制 | C++ | 234 | 1.73 | 17.62 |
| 撒砂控制 | ST | 68 | 1.20 | 8.62 |
| 机车停放制动 | C++ | 201 | 1.27 | 15.34 |
| 里程计算 | ST | 76 | 0.97 | 10.05 |
| 轮缘润滑及测试 | C++ | 384 | 0.77 | 18.08 |

以上仅记录本次已唤醒实例的功能验证表现，不是性能或准确率承诺。报告如实保留需求和边界保护的待复核意见，不将模型审查等同于编译、动态仿真或工程安全验证。
