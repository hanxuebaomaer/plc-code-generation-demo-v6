# Demo v9 验证记录

日期：2026-09-21。范围：软件功能及辅助审查流程，不作代码正确率、真实编译或车辆控制安全验收。

## 自动化验证

35项测试通过：ST/C++语言隔离、完整需求缓存、流式顺序、结构错误不被模型覆盖、六项检查对应、断流与超时、故障兜底范围、普通问候、上传TXT/DOCX/PDF、大小与内容边界、生成后自动修正、改进复核、劣化结果拒绝、歧义需求保留、重新验证不修改代码、v9界面文案。

前端 JavaScript 语法检查通过。

## 界面与模型验证

已检查常见桌面尺寸的三栏一屏布局；代码、需求和验证区域独立滚动。检查六个场景选择、ST/C++切换、生成与同步检查、复制导出入口、需求变更后旧结果失效。

使用已配置的实际模型完成六个场景的生成及审查。接口返回完整代码、六项报告和匹配的代码标识。自动修正只有在复核改善且无新增退步时才采用，无法完成的修正不会伪装为成功。审查结果会随模型输出变化，不承诺每次全部通过。

## 边界

- “初步通过”只表示本次辅助审查未发现具体问题。
- 未使用PLC厂商编译器、真实车辆、硬件台架或安全认证流程。
- 本测试未执行生成代码，不把模型自评作为编译或运行级正确性的证明。
- 公网服务可用性取决于托管平台、网络及模型账户状态；免费实例闲置后可能需要唤醒。
